import { nodeConfig } from "../../types/nodeType";

const config: nodeConfig = {
    type: "code",
    name: "code",
    editName: "codeEdit",
    socketNum: 1,
    plugNum: 1,
    param: {
        name: 123,
    },
    style: {},
};

export { config };

export default config;
