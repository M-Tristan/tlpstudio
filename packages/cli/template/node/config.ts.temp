import { nodeConfig } from "../../types/nodeType";

const config:nodeConfig = {
    type: "{{:name}}",
    name: "{{:name}}",
    editName: "{{:name}}Edit",
    socketNum: 1,
    plugNum: 1,
    param: {
        name: "{{:name}}",
    },
    style: {

    }
};

export { config };

export default config;
